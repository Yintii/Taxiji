<div class="container">
<p style="color: green"><%= notice %></p>

<h1>Wallets</h1>
<hr />

<div id="wallets">
  <% @wallets.each do |wallet| %>
    <%= render wallet, pending_transactions_hash: @pending_transactions_hash %>
  <% end %>
</div>

<%= link_to "New wallet", new_wallet_path %>
</div>
